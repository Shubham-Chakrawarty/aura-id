{
  "name": "aura-id",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . --max-warnings 0 && pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@aura/eslint-config": "workspace:*",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "prettier": "^3.7.4"
  }
}