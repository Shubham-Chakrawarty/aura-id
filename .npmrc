# pnpm configuration to prevent phantom dependencies
# Only hoist essential tooling to the root node_modules
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=@types*

# This ensures that actual logic packages MUST be explicitly
# listed in package.json to be used.
shamefully-hoist=false