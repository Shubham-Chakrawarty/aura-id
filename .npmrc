# pnpm configuration to prevent phantom dependencies
# By default, pnpm hoists types to the root, allowing packages to access them implicitly.
# We override this to only hoist eslint and prettier, forcing explicit type dependencies.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*