# Silence warnings for pnpm-specific settings
loglevel=error
# pnpm configuration to prevent phantom dependencies
# Using pnpm. prefix to stop standard npm from complaining
pnpm.public-hoist-pattern[]=*eslint*
pnpm.public-hoist-pattern[]=*prettier*
pnpm.public-hoist-pattern[]=@types*

# Explicitly disable shameful hoisting
pnpm.shamefully-hoist=false